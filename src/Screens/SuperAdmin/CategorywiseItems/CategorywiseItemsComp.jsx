import React from "react";
import { Outlet } from "react-router-dom";

const CategorywiseItemsComp = () => {
  return (
    <div>
      <Outlet />
    </div>
  );
};

export default CategorywiseItemsComp;
